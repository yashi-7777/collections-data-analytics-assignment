# Collections Analytics Submission

This package contains the reproducible analysis artifacts for the Data Analyst assignment. The analysis deliberately distinguishes facts from causal claims and uses a conservative payment canonicalization.

**Important:** raw data is not bundled in this submission package; place the extracted assignment CSVs under a local raw-data directory and run the notebook/SQL against your warehouse.
